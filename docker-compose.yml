services:
  booksbot:
    build: .
    container_name: booksbot
    restart: always
    env_file: .env
    shm_size: "2gb"
    environment:
      - DISPLAY=:99
    volumes:
      - browser_data:/app/browser_data
      - downloads:/app/downloads

volumes:
  browser_data:
  downloads:
